for idx, loc in enumerate(netAutomata.currentLoc): print("Nœud:", netAutomata.model.nodes[idx].name, "-> État:", loc.name)
for idx, loc in enumerate(netAutomata.currentLoc): print(f"Index: {idx}, Nœud: {netAutomata.model.nodes[idx].name} -> État: {loc.name}")



70 69 68 67 66